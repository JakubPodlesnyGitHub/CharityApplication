namespace Application.Validation.ModuleValidation
{
    public sealed class ModulePropertyValidator
    {
        public ModulePropertyValidator()
        {
            /*RuleFor(x => x.PropertyType)
                .NotNull().WithMessage("Property type is required");
            RuleFor(x => x.PropertyName)
                .NotEmpty().NotNull().WithMessage("Property name is reequired");*/
        }
    }
}